export type Todo = {
  id: string
  user_id: string
  task: string
  is_complete: boolean
  inserted_at: string
}

export type Profile = {
  id: string
  full_name: string
  address: string
  created_at: string
}
